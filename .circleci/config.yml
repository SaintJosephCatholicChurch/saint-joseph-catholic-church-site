version: 2.1
jobs:
  build:
    working_directory: ~/repo
    docker:
      - image: cimg/node:16.17.0
    steps:
      - add_ssh_keys:
          fingerprints:
            - "0e:0c:5d:32:f5:b2:f8:26:04:de:92:b1:3b:65:e9:08"
      - checkout
      - run:
          name: Install Dependencies
          command: yarn
      - run:
          name: Process Bulletins
          command: yarn bulletins-ci

workflows:
  build-deploy:
    jobs:
      - build:
          filters:
            branches:
              only:
                - main
